# =============================================================================
# Configuration CRE Flow
# =============================================================================
# Copier ce fichier en .env et remplir les valeurs
# cp .env.example .env

# =============================================================================
# SonarCloud
# =============================================================================
# Token d'API SonarCloud (générer sur https://sonarcloud.io/account/security)
SONAR_TOKEN=your_sonarcloud_token_here

# Clé du projet SonarCloud (format: organization_repository)
# Exemple: Pluenet-Killian_memTide
SONAR_PROJECT_KEY=your_org_your_repo

# =============================================================================
# WebSocket Server (optionnel)
# =============================================================================
# URL du serveur WebSocket pour les notifications
# WS_BASE_URL=ws://localhost:3001

# =============================================================================
# Git SSH Password (optionnel)
# =============================================================================
# Mot de passe pour sshpass avec les commandes git (push/pull/fetch)
# Si défini, les commandes git seront exécutées avec: sshpass -p "$GIT_SSH_PASSWORD" git ...
# GIT_SSH_PASSWORD=your_password_here

# =============================================================================
# Mode Test (optionnel)
# =============================================================================
# Activer le mode test (1 = actif, 0 = désactivé)
# TEST_MODE=0
